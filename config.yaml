project:
  name: neurocognitive-shared-autonomy

dataset:
  cache_path: data/raw
  subjects: [1]
  runs: [4, 8, 12]
  preload: true

artifacts:
  split_manifest: null
  decoder: null
  calibrator: null

replay:
  source: null

runtime:
  device: cpu
  adaptation_enabled: false
  seed: 42

environment:
  map: null

output:
  directory: results
